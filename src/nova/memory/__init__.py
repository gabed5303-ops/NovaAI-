"""Nova's memory — how it stores and recalls facts, notes, and past events.

`base.py` is the contract, `models.py` is the shape of a saved memory,
`backends/` holds the actual storage methods (a JSON file for now), and
`manager.py` is the friendly front-desk the rest of Nova talks to.
"""
