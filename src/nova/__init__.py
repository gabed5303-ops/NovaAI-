"""Nova — a modular, JARVIS-inspired AI assistant.

This file marks the `nova` folder as a Python "package" (a bundle of code
you can import). The only thing it exposes right now is the version number.
"""

__version__ = "0.3.0"
