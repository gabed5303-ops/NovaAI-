"""Commands: the individual actions Nova can perform.

`base.py` defines what a command is. `registry.py` is the phone book that maps
a command's name to the command itself, and runs it on request. Plugins add new
commands into the registry.
"""
